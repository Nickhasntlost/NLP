# ─────────────────────────────────────────────────────────────────────────────
# Beyond Words — Phase 4: Translation Fine-Tuning Requirements
# Install on the GPU machine BEFORE running train_translation.py
#
# STEP 1 — Install PyTorch with CUDA (pick the right CUDA version for the GPU)
#
#   CUDA 12.1  (most modern RTX 30xx / 40xx / A-series):
#     pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
#   CUDA 11.8  (older RTX 20xx / 30xx):
#     pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
#   After installing PyTorch, verify with:
#     python -c "import torch; print(torch.cuda.is_available(), torch.cuda.get_device_name(0))"
#
# STEP 2 — Install everything below:
#     pip install -r models/requirements_translation.txt
# ─────────────────────────────────────────────────────────────────────────────

# Core HuggingFace stack
transformers>=4.40.0
datasets>=2.18.0
tokenizers>=0.19.0
accelerate>=0.29.0
huggingface_hub>=0.22.0

# PEFT / low-rank adapters (optional but useful for low-VRAM GPUs)
peft>=0.10.0

# IndicTrans2 custom tokenizer & preprocessing toolkit
IndicTransToolkit

# SentencePiece (required by IndicTransToolkit and IndicTrans2 tokenizer)
sentencepiece>=0.1.99

# Evaluation metric
sacrebleu>=2.3.1

# bitsandbytes for optional 8-bit quantisation on very low VRAM (< 8 GB)
bitsandbytes>=0.43.0

# Utility
numpy<2.0
scipy
scikit-learn
tqdm
